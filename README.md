# Küche – Bestell-Oberfläche (GitHub Pages)

Nur die Weboberfläche der Küchen-Bestell-App. Wird von der nativen iPhone-App
(`Kiosk/Kueche/ios`) geladen; gedruckt wird dort nativ an den Bondrucker.

`index.html` bearbeiten + pushen → die App lädt beim nächsten Start die neue
Oberfläche. Keine Geschäftsdaten, kein Server.
